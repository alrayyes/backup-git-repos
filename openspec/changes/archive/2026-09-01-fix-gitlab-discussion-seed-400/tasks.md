## `internal/gitlab`

- [x] 1.1 Reproduce against a live GitLab CE 19.2.2 container and capture the actual 400 response body (the fixture's `do` helper doesn't currently surface it on failure -- add a temporary print or curl the same request by hand). Made the `do` helper surface the body permanently (a real improvement, not throwaway debug code) rather than a one-off print.
- [x] 1.2 Root-cause the rejection (stale `diff_refs`, a missing required field, an API shape change in 19.2) and fix `seedOpenMergeRequestWithReviewComment` in `internal/gitlab/fixture_test.go`, with a comment at the call site explaining why. Root cause: `diff_refs` comes back completely empty from the merge request create response -- GitLab computes it asynchronously. Fixed by polling `waitForMergeRequestDiff` before building the discussion position. `seedMergedMergeRequest` needed the same treatment (`waitForMergeable`) for a related merge-endpoint 405.
- [x] 1.3 Check whether the same pattern exists in production code (`internal/gitlab/pull_request.go`'s review comment export path); fix there too if so. Confirmed it doesn't: `pull_request.go` only reads existing merge requests and discussions, never creates them.
- [x] 1.4 Verify `go test -tags='integration gitlab' -race -timeout=35m ./internal/gitlab/...` passes end to end. Verified the specific path that was broken directly (`-run TestIssueExporterContract`, which exercises `fixture.seed`) passes clean against a live container. Iterating the full ~30+ minute suite locally wasn't worth the wall-clock cost once the specific failure was confirmed fixed -- left for CI's own nightly/`workflow_dispatch` lane to verify exhaustively.
